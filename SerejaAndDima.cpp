#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    int sereja,dima;
    cin >> n;
    int arr[n];
    for(int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    for(int j = 0; j < n; j++)
    {
        if(arr[n] > arr[0])
        {
            sereja++;
        }

    }

}
