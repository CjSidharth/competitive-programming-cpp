#include<bits/stdc++.h>
using namespace std;
int sum(int b)
{
    int n = sizeof(b)/sizeof(int);
}
int main()
{
    int n; cin >> n;
    int a[n];
    for(int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    for(int j = 0; j < n; j++)
    {
        sum(a[j]);
    }
}
