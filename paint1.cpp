#include<bits/stdc++.h>
#define REP(i,a,b) for(int i = a; i < b; i++)
using namespace std;
int main()
{
    int n; cin >> n;
    int price[n];
    REP(i,0,n)
    {
        cin >> price[i];
    }
    int rate[n];
    REP(i,0,n)
    {
        cin >> rate[i];
    }
}
